import 'package:flutter/material.dart';

const bgColor = Color.fromRGBO(0, 51, 102, 1.0);
const fgColor = Color.fromRGBO(255, 255, 255, 1.0);

const defaultPadding = 16.0;

enum MenuOption {
  country,
  load,
  task,
}